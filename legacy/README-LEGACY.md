# Legacy implementation

This directory contains the complete pre-v2 PoseModel repository, moved without rewriting its
contents. It is retained for provenance and reproduction of historical experiments. It is not
imported by the new `posemodel` package and is not covered by the new package's compatibility
guarantees.
